#pragma once
#include <unordered_map>

#include <SFML/Graphics.hpp>

/*struct Square {
	const sf::Vector2i coordinates;
	const int id;
	Square(int x, int y, int _id) : coordinates(sf::Vector2i(x, y)), id(_id) {}
};

struct SimpleGraph {
	std::unordered_map<Square, std::vector<Square>> edges;

	std::vector<Square> neighbors(Square sq) {
		return edges[sq];
	}
};*/

class Grid {
	
};

